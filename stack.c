#include "monty.h"
/**
 * f_stack - prints the leading stack
 * @head: the stacks head
 * @counter: line_numbe
 * Return: no value will be returned
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
